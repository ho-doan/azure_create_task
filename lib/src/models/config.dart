class ScreenShotConfig {
  ScreenShotConfig({
    required this.userName,
    required this.token,
    required this.organization,
    required this.project,
  });
  final String userName, token, organization, project;
}
